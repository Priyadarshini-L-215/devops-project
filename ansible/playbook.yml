- name: Local Deployment
  hosts: local
  become: false

  tasks:
    - name: Install Flask
      pip:
        name: flask

    - name: Create app directory
      file:
        path: /tmp/deployed_app
        state: directory

    - name: Copy app files
      copy:
        src: ../app/
        dest: /tmp/deployed_app/

    - name: Run Flask app
      shell: nohup python3 /tmp/deployed_app/app.py &
      async: 10
      poll: 0
